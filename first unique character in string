class Solution:
    def firstUniqChar(self, s: str) -> int:
        if s == '':
          return -1

        for item in s:
            if s.count(item) == 1:
                return s.index(item)
                break

        return -1
